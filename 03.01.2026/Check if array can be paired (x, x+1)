def canPairConsecutive(arr):
    arr.sort()
    freq = {}

    for x in arr:
        freq[x] = freq.get(x, 0) + 1

    for x in arr:
        if freq[x] == 0:
            continue
        if freq.get(x+1, 0) == 0:
            return False
        freq[x] -= 1
        freq[x+1] -= 1

    return True
